import React from 'react';
import { Layout } from '../../components/Layout';

const Status = () => {
    return (
        <Layout>
            hi from status
        </Layout>
    );
};

export default Status;